name: Continuous Integration
on: push

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Roda um comando!
        run: echo $(pwd)

      - name: Roda outro comando!
        run: echo $(ls)